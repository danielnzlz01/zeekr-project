# Copy to keystore.properties (gitignored) and fill in to enable signed release builds.
# storeType is optional — set PKCS12 for a .p12/.pfx keystore; omit for a .jks/.keystore.
storeFile=/path/to/your/upload-keystore.jks
#storeType=PKCS12
storePassword=
keyAlias=
keyPassword=
